{
  "name": "nova-os",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "NOVA OS \u2014 a new way to use your phone. Docs + Phase 1 interactive prototype.",
  "scripts": {
    "serve": "python3 -m http.server 8080 --bind 0.0.0.0 --directory prototype",
    "check:motion": "bash tools/lint-motion.sh",
    "check:classes": "python3 tools/lint-classes.py",
    "check:springs": "node tools/spring-check.mjs",
    "check:port": "node tools/check-motion-port.mjs",
    "check:experience": "node tools/experience-check.mjs",
    "check:stability": "node tools/ticker-check.mjs && node tools/experience-check.mjs --native && node tools/experience-check.mjs --native --not-default",
    "check:deploy": "node tools/deploy-check.mjs",
    "check:deploy:live": "node tools/deploy-check.mjs --live",
    "check:deploy:selftest": "node tools/deploy-check.mjs --selftest",
    "url:discover": "node tools/deploy-check.mjs --discover",
    "check": "npm run check:motion && npm run check:classes && npm run check:springs && npm run check:port && npm run check:experience && npm run check:stability && npm run check:deploy",
    "golden": "node tools/golden-curves.mjs",
    "apk": "bash tools/stage-assets.sh && cd android && gradle assembleDebug",
    "apk:assets": "bash tools/stage-assets.sh",
    "icons": "bash tools/make-icons.sh",
    "version:bump": "node tools/bump-version.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
